channels:
  - defaults
  - cadquery
  - conda-forge
dependencies:
  - python=3.8
  - pip
  - cadquery=master
  - pip:
      - jupyter-cadquery==3.0.0
      - pmef
      - scipy
      - pyvista
      - meshio
      - ipywidgets
      - pythreejs
